<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ziwow.scrmapp.common.persistence.mapper.FilterChangeRemindMapper">
    <resultMap id="BaseResultMap" type="com.ziwow.scrmapp.common.persistence.entity.FilterChangeRemind">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="productId" property="productId" jdbcType="BIGINT"/>
        <result column="filterId" property="filterId" jdbcType="BIGINT"/>
        <result column="remindTime" property="remindTime" jdbcType="DATE"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
    id, productId, filterId, remindTime, status
  </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List"/>
        from t_filter_change_remind
        where id = #{id,jdbcType=BIGINT}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from t_filter_change_remind
    where id = #{id,jdbcType=BIGINT}
  </delete>
    <insert id="insert" parameterType="com.ziwow.scrmapp.common.persistence.entity.FilterChangeRemind">
    insert into t_filter_change_remind (productId, filterId,
      remindTime, status)
    values (#{productId,jdbcType=BIGINT}, #{filterId,jdbcType=BIGINT},
      #{remindTime,jdbcType=DATE}, #{status,jdbcType=INTEGER})
  </insert>
    <insert id="insertSelective" parameterType="com.ziwow.scrmapp.common.persistence.entity.FilterChangeRemind">
        insert into t_filter_change_remind
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="productId != null">
                productId,
            </if>
            <if test="filterId != null">
                filterId,
            </if>
            <if test="remindTime != null">
                remindTime,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="productId != null">
                #{productId,jdbcType=BIGINT},
            </if>
            <if test="filterId != null">
                #{filterId,jdbcType=BIGINT},
            </if>
            <if test="remindTime != null">
                #{remindTime,jdbcType=DATE},
            </if>
            <if test="status != null">
                #{status,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <insert id="batchSave" parameterType="java.util.List">
        insert into t_filter_change_remind (productId, filterId,
        remindTime, status)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.productId}, #{item.filterId},#{item.remindTime},#{item.status})
        </foreach>
    </insert>
    <update id="updateByPrimaryKeySelective"
            parameterType="com.ziwow.scrmapp.common.persistence.entity.FilterChangeRemind">
        update t_filter_change_remind
        <set>
            <if test="productId != null">
                productId = #{productId,jdbcType=BIGINT},
            </if>
            <if test="filterId != null">
                filterId = #{filterId,jdbcType=BIGINT},
            </if>
            <if test="remindTime != null">
                remindTime = #{remindTime,jdbcType=DATE},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.ziwow.scrmapp.common.persistence.entity.FilterChangeRemind">
    update t_filter_change_remind
    set productId = #{productId,jdbcType=BIGINT},
      filterId = #{filterId,jdbcType=BIGINT},
      remindTime = #{remindTime,jdbcType=DATE},
      status = #{status,jdbcType=INTEGER}
    where id = #{id,jdbcType=BIGINT}
  </update>
    <update id="updateStatus">
        update t_filter_change_remind set status = #{status,jdbcType=INTEGER}
        where status &lt; 3 and productId = #{productId,jdbcType=BIGINT}
    </update>

    <select id="getFilterChangeReminds" resultType="com.ziwow.scrmapp.common.bean.vo.FilterChangeRemindMsgVo">
    	SELECT
			fans.openid openId,fans.wf_nickname nickName,usr.mobilePhone,product.productName productName,product.modelName productSpec,filter.filterName,product.buyTime purchaseDate,remind.remindTime remindDate,1 type
		FROM
			t_filter_change_remind remind,
		  	t_filter filter,
		  	t_product product,
		  	t_wechat_user usr,
		  	t_wechat_fans fans
		WHERE
		    remind.productId = product.id
		    and remind.filterId = filter.id
		    and product.userId = usr.userId
		    and usr.wfId = fans.id
		    and remind.status = 1
			and product.status = 1
		    and fans.is_cancel = 0
			and curdate() = DATE_SUB(remind.remindTime, INTERVAL 30 DAY)
		UNION
		SELECT
			fans.openid openId,fans.wf_nickname nickName,usr.mobilePhone,product.productName productName,product.modelName productSpec,filter.filterName,product.buyTime purchaseDate,remind.remindTime remindDate,2 type
		FROM
			t_filter_change_remind remind,
		  	t_filter filter,
		  	t_product product,
		  	t_wechat_user usr,
		  	t_wechat_fans fans
		WHERE
		    remind.productId = product.id
		    and remind.filterId = filter.id
		    and product.userId = usr.userId
		    and usr.wfId = fans.id
		    and remind.status = 1
		    and product.status = 1
		    and fans.is_cancel = 0
			and curdate() = DATE_ADD(remindTime, INTERVAL 30 DAY)
		UNION
		SELECT
			fans.openid openId,fans.wf_nickname nickName,usr.mobilePhone,product.productName productName,product.modelName productSpec,filter.filterName,product.buyTime purchaseDate,remind.remindTime remindDate,3 type
		FROM
			t_filter_change_remind remind,
		  	t_filter filter,
		  	t_product product,
		  	t_wechat_user usr,
		  	t_wechat_fans fans
		WHERE
		    remind.productId = product.id
		    and remind.filterId = filter.id
		    and product.userId = usr.userId
		    and usr.wfId = fans.id
		    and remind.status = 1
			and product.status = 1
		    and fans.is_cancel = 0
			and curdate() = DATE_SUB(remindTime, INTERVAL 3 DAY)
    </select>

    <select id="findByProductId" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List"/>
        from t_filter_change_remind
        where status in (1,2) and productId = #{productId,jdbcType=BIGINT}
    </select>

    <update id="updateFilterIdsStatus">
        update t_filter_change_remind
        set status = #{status}
        where filterId in
        <foreach item="item" index="index" collection="filterIds" open="(" separator="," close=")">
            #{item}
        </foreach>
        and productId = #{productId}
    </update>

    <delete id="deleteByProductId" parameterType="java.lang.Long">
        delete from t_filter_change_remind
        where productId = #{productId,jdbcType=BIGINT}
    </delete>
</mapper>